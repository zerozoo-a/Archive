### 1. 문제점
- 프론트에서 의존하고 있는 npm 라이브러리 업데이트 도중 알 수 없는 css 깨짐 현상이 발생함.
---

### 2. 문제의 해결
- 확인 방법은 여러 시도 끝에 각 library들이 서로 사용하고 있는 기술 스택을 지원하지 않는 경우에 깨질 수 있다는 것
버려지는 기술이 도입되어 있는 낡은 library는 최우선으로 배제 혹은 대체하여야 한다.
---

### 3. 배운점
- 개인이 만든 소규모 라이브러리는 직접 구현을 하던지 메인 스트림에 있는 라이브러리를 사용하도록 하자.
js로 직접 짠 것이라면 문제가 없었을 것이다. 너무 의존하는 것도 좋지 않다. 간단한 것은
UI의 로직을 배울 겸 직접 짜보도록 하자.

---

### 4. 느낀점

- 답은 docs에 있다. 읽기 싫어도 docs는 지름길이다!
